import React from "react";
import { Button, Flex } from "../components";

const Home = () => (
  <>
    <h1>Home page</h1>
    <Flex flexDirection="column">
      <Button>Oh no</Button>
      <Button>ah</Button>
    </Flex>
  </>
);

export default Home;
